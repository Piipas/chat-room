{
  "name": "@bibas/prisma",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "prisma migrate deploy && prisma generate && tsc"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "main": "dist/index.js"
}
