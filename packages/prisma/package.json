{
  "name": "@bibas/prisma",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "prisma generate && tsc",
    "build:deploy": "prisma migrate deploy && prisma generate && tsc && mkdir -p dist/generated/prisma && cp -R generated/prisma dist/generated/ "
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.18.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "main": "dist/index.js",
  "dependencies": {
    "@prisma/client": "^6.18.0"
  }
}
